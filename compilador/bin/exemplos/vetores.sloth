first
    int a[2], b=3, c[2]; #vetor nao pode ser inicializado#
    c[0] = 1;
    c[1] = 2;

    int d = c[1];

    a[0] = b+c[0];
    a[1] = b*c[1];

    foreach(num:a) begin
        write(num+" "+d+"\n");
    end
last