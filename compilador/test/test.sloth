int fibo(int n) begin
    when(n<2)begin
        return n;
    end otherwise begin
        return fibo(n-1)+fibo(n-2);
    end
end

int soma(int a, int b) begin
    return a+b;
end

double soma2(int a, double b) begin
    return a+b;
end

first
    int a = 0;
    while((a*2)+3/(2*(soma(a+5,1)+2)) < 2.0%+soma2(1,soma(25/3,3*soma(4,5))/5)) begin
        write("test"+fibo(4)+"\n");
        a = a + 1;
    end
last
